#include <gtest/gtest.h>

namespace {

// Integration test: simulate two peers syncing
TEST(PeerSyncTest, TwoPeersSyncFile) {
  // TODO: Create two SyncEngine instances
  // Add folder on peer A
  // Add folder on peer B
  // Create file on A
  // Wait for sync
  // Verify file appears on B
  EXPECT_TRUE(true);
}

}  // anonymous namespace
